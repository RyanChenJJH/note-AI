# 07 · 开发路线图

> 状态图例：⬜ 未开始 · 🟦 进行中 · ✅ 完成

## 阶段 0 · 设计与文档 ✅
- ✅ 确认数据源（Claude Code + Codex CLI 本地 jsonl）并实测格式
- ✅ 确认技术栈（React+Vite+Tailwind+shadcn / Node 后端）
- ✅ 确认视觉风格（晨雾 Sage Mist）
- ✅ 确认导出目录与 AI 接口取向
- ✅ 产出 docs/01–07 文档方案

## 阶段 1 · 后端内核（先跑通数据） ✅
> 目标：命令行就能验证"扫描 → 解析 → 分轮 → 导出 md"。
> 实现见 `server/`；施工蓝图与 as-built 见 `docs/08`。TDD，21 个测试全绿。
- ✅ `scanner`：扫描两个源目录，输出会话清单（id/source/title/day/turnCount），排除 subagents/
- ✅ `parser`：单会话解析 + 过滤 + 分轮（严格按 `docs/03`，Claude+Codex 全规则覆盖）
- ✅ `exporter`：选中块 → 带 front matter 的 md（按 `docs/05`）
- ✅ `manifest`：已导出索引读写 + 状态推导
- ✅ 自测：本机真实会话核对（139 会话扫描；轮数/噪音过滤/标题/`</think>`裁剪/导出 均通过 §F）

## 阶段 2 · HTTP 接口 ✅
> 目标：把内核包一层 `node:http`（零依赖）。实现见 `server/src/http/` + `src/config.ts`；蓝图与 as-built 见 `docs/09`。TDD，31 个测试全绿。
- ✅ Node 服务 + 路由：`/api/sources`、`/api/sessions`、`/api/sessions/:id`、`/api/export`、`/api/config`
- ✅ 时间范围筛选（7d/30d/all）在 scanner 层实现（`filterByRange`）
- ✅ 会话 `exported` 状态由 manifest 注入；`/api/sessions/:id` 块级 `exported` 也注入
- ✅ 错误处理与容错（404/405/400/500，进程不崩；只读源不变）
- ✅ 真实数据手测：sources 47+93、range 筛选、导出+manifest 回写均通过（`docs/09 §11`）

## 阶段 3 · 前端框架与封面 ✅
> 目标：把 `web/` 立起来，主题/封面/路由成立。实现见 `web/`；蓝图与 as-built 见 `docs/10`。
- ✅ Vite 8 + React 19 + Tailwind v4（CSS-first `@theme`）+ shadcn；晨雾配色 token 落 `src/index.css`
- ✅ 字体接入（Lora/Raleway + Noto SC，Google CDN `@import`）
- ✅ 封面页（呼吸光晕 GlowOrb + 鼠标视差 + 青色「进入」CTA；reduced-motion 全停）
- ✅ 路由：封面 `/` → `/app`（crossfade ≤280ms）；Vite 代理 `/api`→127.0.0.1:8787
- ✅ 真实验证：`npm run build` 零错零警告；`/app` 经代理拿到 Claude 47 · Codex 93（`docs/10 §11`）

## 阶段 4 · 主界面（两栏） ✅
> 目标：可用两栏工作界面，跑通选源→选会话→读分轮→勾块→命名→导出→回写。实现见 `web/`；蓝图与 as-built 见 `docs/11`。
- ✅ 左栏：Codex/Claude 分类 Tab（点击懒加载）+ 时间范围筛选（7d/30d/all）
- ✅ 左栏：按日期分组、可折叠列表 + 已导出徽章（●/◐）
- ✅ 右栏：完整对话阅读区（按轮分块、可滚动）+ 思考过程开关
- ✅ 右栏：每块独立 checkbox + 块级已导出标注
- ✅ ExportBar：文件名输入 + AI 整理开关（占位/降级提示）+ 导出按钮 → `POST /api/export`
- ✅ 微动效：列表 stagger、悬停轻浮、选中态青色描边（`docs/04` §3）
- ✅ reduced-motion / focus / 对比度 可达性过检
- ✅ 真实数据验证：Claude 47·Codex 93 懒加载、范围筛选、导出回写均通过；纯逻辑 6 单测 + build 零错（`docs/11 §11`）

## 阶段 5 · AI 整理占位 ✅
> 目标：建好 tidy() 接缝，换真实模型时只动 `server/ai/`。实现见 `server/src/ai/`；蓝图与 as-built 见 `docs/12`。
- ✅ `server/src/ai/tidy.ts` 占位实现（原样拼接、`usedAi:false` 降级）；配置 schema 阶段 2 已就位
- ✅ 导出链路接入 `tidy()`：`aiTidy` 开走占位、关走原路；两路正文一致；manifest/响应记 `usedAi`；前端按真实结果提示降级
- ✅ exporter 重构（buildBody/buildFrontMatter，输出不变）；后端测试 35 全绿、前端 build 零错（`docs/12 §11`）

## 阶段 6 · 打包与一键启动 ✅
> 目标：`server` 同源托管 `web/dist` + API；`start.cmd` 一键起服+开浏览器。蓝图与 as-built 见 `docs/13`。
- ✅ `server/src/http/static.ts` + server.ts 接入：API 未命中 → 文件 → SPA fallback；`/api/*` 不被吞
- ✅ `paths.staticDir()`（默认 `web/dist`，`AIDA_STATIC_DIR` 可覆盖）+ `loadConfig` 让 `AIDA_EXPORT_DIR` 覆盖 `cfg.exportDir`
- ✅ `start.cmd`：缺 `web/dist` 才构建；独立最小化窗口跑 serve；探测 `/api/sources` 就绪后 `start "" http://127.0.0.1:8787`
- ✅ README 增「快速开始」段；后端测试 40 全绿（原 35 + 静态 5）

## 阶段 7 · AI 真实接入 ✅
> 目标：占位 `tidy()` → 真实模型 + 真实 skill，把勾选对话整理成多篇 Obsidian 知识笔记，预览确认后写入。实现见 `server/src/ai/` + 前端设置/预览；蓝图与 as-built 见 `docs/15`。
- ✅ OpenAI 兼容 provider（默认 DeepSeek）；内联 `SKILL.md`+`type-selection`+12 模板进 system prompt；哨兵块解析多篇
- ✅ 契约改多篇 `notes:[{filename,content}]`（自带 frontmatter / AI 命名 / 可 0 篇）；未配置/超时/鉴权/无法解析全降级
- ✅ `/api/ai/preview`(不写盘) + `/api/ai/commit`(写 `aiNotesDir`) 预览-确认流；手动 `/api/export` 回归原样
- ✅ 两目录拆分（手动→`00_收件箱`、AI→`02_AI对话记录`）+ 设置界面（provider/key脱敏/model/温度/超时/两目录/skill 路径）
- ✅ 后端 62 测试全绿、前端 `build` 零错、真机冒烟通过；⏳ 真实 DeepSeek key 联调待用户

## 后续迭代（非本期） ⬜
- ⬜ Anthropic / Ollama provider 真实接入（同 `ChatProvider` 形状；`docs/06` §5）
- ⬜ 暗色模式
- ⬜ 列表虚拟化（会话量大时）
- ⬜ 导出模板可配置（标题层级 / 标签 / front matter 字段）

---

## 建议的推进顺序
**先做阶段 1（后端内核）并用真实数据自测**——这是风险最高、价值最核心的部分。数据能正确解析出来后，接口、界面、动效都是顺势而为。
